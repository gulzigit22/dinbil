import 'package:flutter/material.dart';

class KurbanAit extends StatelessWidget {
  const KurbanAit({super.key});

  @override
  Scaffold build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('KurbanAit'),
      ),
    );
  }
}
