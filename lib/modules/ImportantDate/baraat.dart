import 'package:flutter/material.dart';

class Baraat extends StatelessWidget {
  const Baraat({super.key});

  @override
  Scaffold build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Baraat'),
      ),
    );
  }
}
